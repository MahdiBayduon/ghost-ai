# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Design system setup

## Current Goal

- Define the immediate implementation goal here.

## Completed

- Design system and UI primitives (`context/feature-specs/01-design-system.md`):
  - shadcn/ui initialized (`components.json`, style `base-nova`, Base UI primitive library).
  - Added components: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (`components/ui/*`, unmodified from generated output).
  - `lucide-react` installed (pulled in automatically by shadcn init as the configured icon library).
  - `lib/utils.ts` created with `cn()` (clsx + tailwind-merge), generated by shadcn init.
  - `app/globals.css` rewritten: added the full dark palette from `context/ui-context.md` as CSS custom properties, mapped shadcn's semantic tokens (`--background`, `--card`, `--primary`, `--border`, etc.) onto them, and added `@theme inline` utilities for the custom token names named in `context/code-standards.md` (`bg-base`, `bg-surface`, `text-copy-primary`, `text-copy-muted`, `border-surface-border`, `text-brand`, `bg-accent-dim`, plus the remaining palette entries following the same naming pattern).
  - `app/layout.tsx`: added a permanent `dark` class on `<html>` since the app is dark-only (no theme toggle), which also activates the `dark:` variant refinements baked into the generated `components/ui/*` files.
  - Verified: clean `tsc` type-check (ignoring one pre-existing, unrelated stray global `@types/testing-library__jest-dom` package outside the repo), and a temporary smoke-test page importing all 7 components rendered with no errors and the correct dark palette in compiled CSS.

## In Progress

- None yet.

## Next Up

- Add the next planned feature unit here.

## Open Questions

- The custom Tailwind token names for the remaining palette entries not given as literal examples in `context/ui-context.md` / `context/code-standards.md` (e.g. `bg-elevated`, `bg-subtle`, `text-copy-secondary`, `text-copy-faint`, `text-ai`, `text-error`/`success`/`warning`) were inferred by following the same pattern as the given examples. Confirm this naming is acceptable before relying on it broadly in future components.

## Architecture Decisions

- Dark theme is forced via a permanent `dark` class on `<html>` rather than `prefers-color-scheme`, since the product is dark-only with no light mode.
- shadcn/ui is configured with the `base-nova` style, which uses Base UI (`@base-ui/react`) rather than Radix UI as the underlying primitive library.

## Session Notes

- Add context needed to resume work in the next session.
